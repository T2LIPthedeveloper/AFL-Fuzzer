# Spring Fuzzer

The Spring Boot rebuild lives in `spring-fuzzer/`.

Branch ladder for review experiments:
- `main-spring` — baseline microservice
- `develop-1-spring` — small changes
- `develop-2-spring` — medium changes
- `develop-3-spring` — large changes

All spring develop PRs target `main-spring`.
